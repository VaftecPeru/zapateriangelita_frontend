name: Validate premium home

on:
  push:
    branches:
      - feature/home-premium-commerce-experience

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
      - run: npm ci
      - run: npm run build
      - name: Verify dist
        run: |
          test -f dist/index.html
          test -d dist/assets
          echo "Premium home build OK"
